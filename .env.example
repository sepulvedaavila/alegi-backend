# Supabase

SUPABASE_URL=              # Your Supabase project URL
SUPABASE_SERVICE_KEY=      # Service role key (full access)
SUPABASE_WEBHOOK_SECRET=   # For webhook signature verification

# OpenAI

OPENAI_API_KEY=            # Your OpenAI API key
OPENAI_MODEL=              # Optional: defaults to 'gpt-4-turbo-preview'

# Sentry

SENTRY_DSN=                # Your Sentry project DSN

# Email Configuration

SMTP_HOST=                 # SMTP server host
SMTP_PORT=                 # SMTP port (default: 587)
SMTP_SECURE=               # Use TLS (true/false)
SMTP_USER=                 # SMTP username
SMTP_PASS=                 # SMTP password
FROM_EMAIL=                # Sender email address

# Application Settings

NODE_ENV=                  # 'production' or 'development'
FRONTEND_URL=              # Frontend URL for email links
EXTERNAL_WEBHOOK_SECRET=   # Secret for external webhook verification

# PDF CO
PDF_CO_API_KEY=            # your PDF.CO API key

# Vercel-specific (auto-populated)

VERCEL_GIT_COMMIT_SHA=     # Used by Sentry for release tracking